import { createRouter, createWebHistory } from "vue-router";

const router = createRouter({
    history: createWebHistory(),
    routes: [
        // { path: "/nav", component: NavigationDrawer },
        // { path: "/tool", component: Toolbar },
    ],
});
export default router;
